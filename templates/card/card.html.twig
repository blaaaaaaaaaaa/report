{% extends "base.html.twig" %}

{% block title %}Card{% endblock %}

{% block content %}
<h1 class="arubrik">Kortspel</h1>
<div class="content">
    <a href="{{ allCards }}">Deck</a>
    <a href="{{ path("draw") }}">Draw</a>
    <a href="{{ drawNum }}">Draw two or more</a>
    <a href="{{ shuffle }}">Shuffle</a>
    <a href="{{ game }}">Game</a>
    <a href="{{ cardDeck2 }}">Deck jokers</a>
    <a href="{{ deckAPI }}">Deck JSON</a>
    <a href="{{ shuffleJson }}">Shuffle JSON</a>
</div>

{% endblock %}
