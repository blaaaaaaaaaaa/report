{% extends "base.html.twig" %}
<title>Shuffle</title>
<body>

    <div class="content-cards">

        <h1>Shuffle</h1>

<p>
{# Shuffled cards = {{ shuffled_cards }} #}

</p>
        <div class="card">
            {% for card in shuffled_cards %}
            <div class="cards">
                {{ card.getCard() }}

            </div>
            {% endfor %}
        </div>
    </div>

</body>
</html>

{% endblock %}